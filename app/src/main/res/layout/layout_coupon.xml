<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:id="@+id/drawMene"
    android:layout_height="match_parent"

    tools:context=".couponActivity">



    <!-- Qui include un layout definito in precedenza -->
    <include layout="@layout/activity_coupon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- Qui sotto inserisci il layout del Navigation Drawer -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/menulaterale"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/backmenuvertica"
        app:itemIconSize="50dp"
        app:itemHorizontalPadding="10dp"
        app:itemVerticalPadding="10dp"
        app:headerLayout="@layout/coustum_layout_menu_laterale"
        android:backgroundTint="@color/giallochiaro"
        app:drawerLayoutCornerSize="50dp"
        app:menu="@menu/menu_home_laterale"/>

</androidx.drawerlayout.widget.DrawerLayout>